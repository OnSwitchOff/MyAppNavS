<Page
    x:Class="MyAppNavS.MyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyAppNavS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--<Page.Resources>
        <Style x:Key="NavigationViewStyle" TargetType="NavigationView">
            <Setter Property="Background" Value="{ThemeResource ApplicationPageBackgroundThemeBrush}" />
            <Setter Property="IsTitleBarAutoPaddingEnabled" Value="True" />
            <Setter Property="IsSettingsVisible" Value="true" />
            <Setter Property="IsBackButtonVisible" Value="Collapsed" />
            <Setter Property="IsBackEnabled" Value="False" />
            <Setter Property="CompactModeThresholdWidth" Value="640" />
            <Setter Property="ExpandedModeThresholdWidth" Value="1008" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="AlwaysShowHeader" Value="True" />
            <Setter Property="IsPaneToggleButtonVisible" Value="True" />
            <Setter Property="AlwaysShowHeader" Value="True" />
            <Setter Property="Background" Value="LightSeaGreen"/>
        </Style>
    </Page.Resources>
    <NavigationView x:Name="navigationView"
        IsBackButtonVisible="Visible"
        Style="{StaticResource NavigationViewStyle}">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Home" Background="PaleVioletRed" />
            <NavigationViewItem Content="Customers"/>
            <NavigationViewItem Content="Orders"/>
            <NavigationViewItem Content="Products"/>
            <NavigationViewItem Content="Activity Log"/>
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <NavigationViewItem Content="Home"></NavigationViewItem>
            <NavigationViewItem Content="Customers"/>
            <NavigationViewItem Content="Orders"/>
            <NavigationViewItem Content="Products"/>
            <NavigationViewItem Content="Activity Log"/>
        </NavigationView.FooterMenuItems>

        <Frame x:Name="frame" Background="BurlyWood" />
    </NavigationView>-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="btn" Background="LightSeaGreen">Click</Button>
        <TextBox Text="{x:Bind ViewModel.Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>

</Page>
